<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wsfmn.habittracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".HabitHistoryActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".ViewHabitEventActivity" />
        <activity
            android:name=".HabitListViewActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".HabitListViewDetailActivity"
            android:parentActivityName=".HabitListViewActivity" />
        <activity
            android:name=".addNewHabitActivity"
            android:parentActivityName=".HabitListViewActivity" />
        <activity android:name=".DaysOfTheWeekActivity" />
        <activity
            android:name=".HabitsForTodayActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>